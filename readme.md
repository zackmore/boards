# Boards

Just a demo page.

## Structure

```
app
    user_data   // Store the temp data in editing
    config      // Initial configuration
    ui          // Events bindings in UI
        page_menu
            init
            event bindings
        widget_menu
            init
            event bindings
        canvas
            widgets render
            event bindings
    page
        parse
        new
        delete
    widget
        parse
        new
        delete
    helper      // Load/save data from/to remote server
    init
```
